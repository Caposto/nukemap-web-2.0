/**
 * Tabletop prototype with Hit-Testing (Maybe Anchoring) using WebXR, ThreeJS, A-Frame
 */

import registerHideComponent from "../components/hide.component";

window.onload = () => {
    registerHideComponent()
}